# ADMIN-1G RBAC Report

## SUPPORT (Customer Service)

`support:view|reply|assign` + players/wallet/deposit/withdraw **view**  
No PII, no admins:manage, no risk:manage, no wallet adjust.

## OPS

+ `support:*`, `admins:sessions:view`, `security:view`

## Admin management

Still `admins:view` / `admins:manage` (SUPER only for manage today).

Backend enforces all routes; frontend hide insufficient.
