import AdminApp from "../admin-app.tsx";

/** Dedicated /admin/login URL — same AdminApp shell (LoginScreen when unauthenticated). */
export default function AdminLoginPage() {
  return <AdminApp />;
}
