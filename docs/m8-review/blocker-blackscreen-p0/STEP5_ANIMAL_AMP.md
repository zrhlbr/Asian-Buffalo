# STEP5_ANIMAL_AMP.md

## Status: NOT re-applied this incident

Prior animal-life amplitude in `symbol-life.ts` / `buffalo.ts` was already present and **did not** reproduce the black main viewport (MeshBasic tiles visible in before/after captures).

Per Rule V1.1 Step 5: only after full stable restore, and only if needed. Re-boosting amplitude now would violate the freeze without new evidence.

If Zhao wants stronger amp later: re-open a dedicated presentation task with smaller deltas; keep MeshBasic; no ShaderMaterial reel tiles.
