# I18N — Auth P3

Locales: **zh-CN / en / my-MM** (lobby dict in `client/xi-lobby/i18n.ts`).

## Auth keys (all three languages)

- Titles: `auth.title.login|register|forgot`
- Tabs: `auth.tab.phone|email`
- Fields: phone/email/password/otp/nickname
- CTA / navigation: sendOtp, verify, submitLogin/Register/Reset, goLogin/Register/Forgot
- Errors: `auth.error.*` including `INVALID_*`, `OTP_*`, `LOGIN_RATE_LIMITED`, `PROVIDER_NOT_CONFIGURED`, `PROVIDER_NOT_PRODUCTION_READY`, `PASSWORD_MISMATCH`, …
- Lobby chrome: `lobby.auth.login|register|logout|forgot`

## Gate

`assertLobbyI18nComplete()` — **PASS** via `tests/xi-auth-i18n.test.mjs`.

## Admin

`players.registeredAt` added zh/en/my in `lib/admin/i18n.ts`.
