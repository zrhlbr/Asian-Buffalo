# BLOCKED_CAPTURE — Auth P3 screenshots

**Status:** BLOCKED

**Reason:** No headed browser / device capture session available in this agent execution. Mobile smoke (320–430) UI was implemented (`auth-app` + CSS) but visual screenshots were not taken.

**To capture later:**

1. `AB_AUTH_OTP_TEST_MODE=1` + local `vinext`/`vite` dev
2. Open `/xi/register`, `/xi/login`, `/xi/forgot` at 390×844
3. Phone + email tabs; OTP step with test hint; lobby topbar login/register buttons
4. Store under `docs/m8-review/xi-game-v2/auth-p3/screenshots/`
