# I18N — Step 4

## Player (lobby / hub)

Additive `lobby.commerce.*` keys in `client/xi-lobby/i18n.ts` for **zh-CN / en / my-MM** (parity).

## Slot HUD overlays

Additive `walletAvailable` / `walletFrozen` / `walletStatus` / `walletNoMoves` in `client/m5/i18n.ts` (zh/en/my).

## Admin

Additive `nav.deposits|withdrawals|activities` + module strings in `lib/admin/i18n.ts` (zh/en/my).  
`tests/r1-m7-admin.test.mjs` i18n identical-key-set **PASS**.
