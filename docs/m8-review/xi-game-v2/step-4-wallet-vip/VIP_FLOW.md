# VIP_FLOW

Reuse screenshot-features VIP services:

- `vip_level_config` levels 1–6 — admin upsert conditions JSON (BR-001 PENDING placeholders)
- `player_vip` level/status/expiry — admin assign with reason
- Hub/lobby VIP center: `GET /api/v1/game/vip/levels` real data
- Play surface: VIP badge only (Step 3 contract) — no VIP modal from play stuffing

Upgrade thresholds are **not** hardcoded from African Buffalo screenshots.
