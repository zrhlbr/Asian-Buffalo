# 《西游戏》V2.0 第一阶段开发报告（大厅）

**模块：** 西游戏 / XI GAME 平台大厅（Phase 1 ONLY）  
**日期：** 2026-08-07  
**状态：** 待赵总验收 — **未开始 Phase 2 / Phase 3**  
**约束：** 未 commit / push / merge / deploy

---

## 1. 交付结论

已完成**加法式**平台大厅，不改动现有可玩老虎机内核（转轴 / 数学 / 钱包账本 / `#gl` 叠层修复）。

| 验收项 | 结果 |
|--------|------|
| 大厅入口 | `/xi` |
| BDK 枢纽桩 | `/xi/bdk` → `/game` 或 `/` |
| 原槽机路由保留 | `/` 仍为 GameClient |
| 槽机别名 | `/game`（同栈） |
| 三语文案 | zh-CN / en / my-MM 全量新 key |
| Hero 资源 | **ASSET_GAP**（占位渐变 + 标签） |
| Phase 2 | **未开始** |
| 工程门禁 | Build PASS；Lobby ESLint PASS；Unit 5/5 PASS；Playwright smoke PASS |

---

## 2. 路由一览

| URL | 说明 |
|-----|------|
| `http://<host>/xi` | 西游戏大厅（平台首页） |
| `http://<host>/xi/bdk` | 牛魔王枢纽桩（非 Phase 2 主页） |
| `http://<host>/` | 现有可玩槽机（保留） |
| `http://<host>/game` | 槽机加法别名 |
| `GET /api/v1/lobby/catalog` | 大厅目录配置（静态种子） |

本地验证示例（Vite）：`http://127.0.0.1:5174/xi`

---

## 3. 功能清单

### 3.1 Hero
- 全宽 hero；慢云 / 日光粒子 / 微呼吸动画（`prefers-reduced-motion` 可关）
- 未找到赵总提供的西游 HD 主视觉 → 见 `ASSET_GAP.md`

### 3.2 顶栏
- 左：头像 / 昵称 / VIP（接 `/api/v1/game/profile`，失败显示游客/错误）
- 中：MMK（接 balance API，与槽机同口径整数展示）+ USDT **暂不可用**（多币种合约待定，非假余额）+ 充值+
- 右：消息 / 公告 / 设置 / 语言（均可点）

### 3.3 游戏目录
- 分类：推荐 / 热门 / 上新 / 奖池 / 老虎机
- **牛魔王** live 卡 → `/xi/bdk`
- 孙悟空 / 哪吒 / 龙王 / 红孩儿 / 白骨精 → coming-soon 弹层（非死链）

### 3.4 功能入口（均可点）
充值、提现、VIP、活动、公告、排行榜、签到、客服、邀请、任务、邮件、礼包、兑换码  
→ 真实弹层；未完成业务规则显示 i18n「业务规则待定」

### 3.5 底栏
首页 / 游戏 / 钱包 / 活动 / 我的 — 大厅壳内切换

---

## 4. 门禁与回归

详见 `GATE_RESULTS.md`。摘要：

- `npx vinext build` **PASS**（路由表含 `/xi` `/xi/bdk` `/game`）
- Lobby ESLint **PASS**
- `tests/xi-lobby-phase1.test.mjs` **PASS**
- Playwright smoke **PASS**（截图见 `screenshots/`）
- `/` 截图确认槽机仍可进入（`03-slot-root.png`）
- P0：`#gl translateZ(0)` 保留；无 shell `#hud { transform: translateZ(0) }`

**诚实阻塞：**

- 全仓 `npm run lint`：Windows 上 `sites-env.sh` CRLF 预存问题
- 全仓 `tsc`：预存 admin/db/examples/worker 错误（大厅文件无新增 tsc 报错）
- `vinext start`：预存 worker `env` undefined

---

## 5. Admin / 合约说明（Rule 6）

- 本阶段**未改** Admin / M9
- 大厅读 `GET /api/v1/lobby/catalog` 静态种子
- CONTRACT：后续 Admin「大厅目录」可同 schema 写入；USDT 多币种余额待正式合约

---

## 6. 交付物索引

目录：`docs/m8-review/xi-game-v2/phase-1/`

1. `MODULE_IMPACT_ANALYSIS.md`（编码前）
2. 本报告
3. `FILE_LIST.md` / `REVIEW_PATCH.md` / `AB-XI-GAME-V2-PHASE1-review.patch`
4. `SHA256.txt` / `RISK.md` / `ROLLBACK.md` / `GATE_RESULTS.md`
5. `I18N_KEYS.txt` / `ASSET_GAP.md`
6. `screenshots/` + `_smoke-lobby.mjs`

---

## 7. 请赵总验收关注点

1. `/xi` 是否作为平台入口可接受（当前 `/` 仍为槽机，避免回归）
2. Hero 占位是否接受，或提供西游 HD 图路径
3. 功能入口「业务规则待定」文案是否可过 Phase 1
4. **确认后再开 Phase 2（牛魔王英雄主页）**

---

## 8. 明确声明

- **Phase 2 NOT started**
- **Phase 3 NOT started**
- **No commit / push / merge / deploy**
